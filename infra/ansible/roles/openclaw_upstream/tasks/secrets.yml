---
# Deploy secrets as an env file for Docker Compose

- name: Deploy upstream environment file
  ansible.builtin.template:
    src: upstream.env.j2
    dest: "{{ openclaw_upstream_root }}/upstream.env"
    mode: "0600"
  no_log: true
  notify: restart openclaw-upstream
